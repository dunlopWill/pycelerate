# TODO

- [ ] gh variable set DOCS_DEPLOYMENT_ENABLED --body true plus Settings → Pages → source "GitHub Actions". Until then the README's docs links 404.
- [ ] PyPI trusted publisher + a pypi environment with required reviewers, or publish.yml fails on first tag.
- [ ] Nothing is pushed. CI hasn't run yet — it triggers on PRs and pushes to main, and you're on dev.
